services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: motora
      POSTGRES_PASSWORD: motora
      POSTGRES_DB: motora
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
